Put your logo here as logo.png.

TitleScreen.js, OnboardingScreen.js and a couple of pack faces import
require('../../assets/logo.png'). Metro will fail on a missing asset, so this
file must exist before the first run.

The design file uses a 1024x1024 PNG with transparency.
